import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('Tests For Our App', () =>{
    it('Knows that racecar backwards is racecar', () =>{
        expect(racecar).toEqual(racecar);
    })
})